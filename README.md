# Onre Dashboard Clone

## Run
1. Install dependencies: `npm install`
2. Start dashboard: `npm run dev`
3. Open `http://localhost:5173`

## Included
- Dashboard layout clone (header, KPI cards, wallet table)
- Wallet Directory movement chart
- Automatic Onre points rollover once per new UTC+1 day
